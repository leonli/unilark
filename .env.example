# Prefer `unilark setup` for hidden credential entry / 推荐通过 setup 隐藏输入凭据。
# 如手动保存到 ~/.unilark/lark.env，权限须为 0600；不要提交实际凭据。
# 这是数据文件，不能 source。值不要加引号或行内注释。
# edition 填 lark（国际版）或 feishu（中国版）。
UNILARK_LARK_EDITION=
UNILARK_LARK_APP_ID=
UNILARK_LARK_APP_SECRET=
